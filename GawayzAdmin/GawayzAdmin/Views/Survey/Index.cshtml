
@{
    ViewBag.Title = "Index";
}

<p>
    @Html.ActionLink("Create New", "Create", new { productId = (int)ViewBag.productId }, new { @class = "btn btn-success", data_modal = "" })
</p>
@{Html.RenderAction("SurveyList", new { productId = (int)ViewBag.productId});}

